# -----------------------------
# Public DNS / TLS (Caddy ACME)
# -----------------------------
DOMAIN=example.com
# По умолчанию: Let's Encrypt STAGING CA (безопасно для тестов, не тратит rate limits).
ACME_CA=https://acme-staging-v02.api.letsencrypt.org/directory

# -----------------------------
# App
# -----------------------------
DEBUG=0
CONFIG_SOURCE=file
SMS_PROVIDER=mock
LLM_PROVIDER=mock

# Front -> API (внутренний URL внутри docker network)
FRONT_BACKEND_URL=http://api:8000

# -----------------------------
# Admin auth
# -----------------------------
# CSV, например: "+79990000000,+78880000000"
ADMIN_PHONE_ALLOWLIST=
# Соль (строка). Пример: "staging-2026-01-07-change-me"
ADMIN_PASSWORD_SALT=
# Hex sha256(salt + password). Не храните пароль в env.
ADMIN_PASSWORD_HASH=
# Опционально
ADMIN_SESSION_TTL_HOURS=12

# -----------------------------
# Postgres
# -----------------------------
POSTGRES_PASSWORD=

# -----------------------------
# Redis
# -----------------------------
REDIS_URL=redis://redis:6379/0

# -----------------------------
# MinIO (S3)
# -----------------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=

# Внутренний endpoint (контейнер -> контейнер)
S3_ENDPOINT=http://minio:9000
# Внешний endpoint для presign URL (публичный домен через Caddy)
S3_PRESIGN_ENDPOINT=https://s3.example.com

S3_PROVIDER=s3
S3_REGION=us-east-1
S3_BUCKET=nayti-lyudey
S3_USE_SSL=0

# -----------------------------
# Render service
# -----------------------------
RENDER_URL=http://render:8000
RENDER_TIMEOUT_SEC=120
RENDER_LOG_LEVEL=INFO

# -----------------------------
# Alerts (опционально)
# -----------------------------
ALERT_WEBHOOK_URL=
