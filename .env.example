# Admin auth (no real values committed)
# Comma-separated E.164 phone allowlist.
ADMIN_PHONE_ALLOWLIST="+79991234567,+79990000000"

# PBKDF2-HMAC-SHA256(password, ADMIN_PASSWORD_SALT, 100k) -> hex
ADMIN_PASSWORD_HASH=""

# Salt used for password hash and admin token hashing (keep secret)
ADMIN_PASSWORD_SALT=""

# Admin session TTL
ADMIN_SESSION_TTL_HOURS=12

# --- LLM (local) ---
# По умолчанию LLM выключен (backend уйдёт в mock), пока не задано и base_url, и ключ.
# Чтобы включить реальный LLM локально:
# 1) выставьте LLM_PROVIDER=openai_compat (опционально, можно оставить пустым для auto)
# 2) задайте LLM_BASE_URL и LLM_API_KEY

# Рекомендуемый режим: явный провайдер
LLM_PROVIDER=openai_compat

# Любой OpenAI-compatible endpoint.
LLM_BASE_URL=https://api.openai.com/v1

# Ключ провайдера (НЕ коммитьте реальные значения)
LLM_API_KEY=__PUT_KEY_HERE__

# Модель (пример)
LLM_MODEL=gpt-4o-mini
